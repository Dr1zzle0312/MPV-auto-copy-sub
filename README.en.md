AI-generated script. Works with [https://github.com/HIllya51/LunaTranslator](https://github.com/HIllya51/LunaTranslator) to perform real-time subtitle-to-speech conversion or real-time translation.

Press `Ctrl+Shift+C` to turn on auto-copy, which automatically copies subtitles to the clipboard. Adjacent deduplication, blocking of repeatedly duplicated subtitles and keyword filtering are enabled by default. You may remove these functions from the script if unwanted.

Press `Ctrl+C` for a one-time manual copy (ignores all the rules above).

Used AI translation
